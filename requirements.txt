# Native pipeline dependencies

# Core body2colmap library (provides Camera, used by pipeline/dataset.py)
body2colmap @ git+https://github.com/Erant/body2colmap.git

numpy>=1.24.0
opencv-python>=4.8.0
PyYAML>=6.0
requests>=2.31.0

# detect_face_landmarks needs MediaPipe. CPU-only, so it installs anywhere;
# left out of the hard requirements because every other step works without
# it and it pulls a large dependency tree.
#   pip install mediapipe
#
# The Gaussian-splat steps (load_splat / save_splat / render_splat) need
# `plyfile` for PLY I/O — that's it. render_splat's rasterisation shells out
# to the brush-splat-render binary (see pipeline/steps/splat.py), not
# body2colmap's gsplat-based SplatRenderer, so body2colmap's "splat" extra
# (which bundles gsplat alongside plyfile, and needs a CUDA toolchain to
# build) is no longer needed here at all:
#
#   pip install plyfile

# The web UI (pipeline/webui.py) — `python -m pipeline.cli ui`. Not a hard
# requirement because every CLI path works without it; the Docker image
# installs it, since serving the UI is what the container does by default.
#
#   pip install 'gradio>=5.0,<7.0'
