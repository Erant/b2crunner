# Per-machine registry of isolated environments a workflow can dispatch into.
# Keyed by the name a StepSpec's `env:` field references. Not committed with
# real paths for every model yet — fill these in as each isolated venv is
# built (see steps/stubs.py for which steps need which env).
#
# envs:
#   wan22:
#     kind: subprocess
#     python_bin: envs/wan22/bin/python
#   sam3dbody:
#     kind: subprocess
#     python_bin: envs/sam3dbody/bin/python
#   seedvr2:
#     kind: docker
#     image: b2c/seedvr2:latest

envs: {}
